用科技风格绘制一个基于RAG的AI大模型计算系统架构，需要有设计感，使用SVG绘制

要求按以下流程进行绘制，但图中显示的术语需要专业：
1. 用户输入Query
2. 根据用户的Query查询RAG数据库FAISS，匹配出相似的查询用例，用例包含Query和具体的SQL语句
3. Agent根据用户的Query和查询到的用例构造Prompt
4. 使用构造的Prompt调用大模型Deepseek，deepseek会返回具体的SQL语句
5. 校验deepseek生成的sql语句是否合法
6. SQL语句合法则在sqllite数据库执行SQL语句并将结果返回，不合法则提示用户当前系统正在繁忙，请稍后再试
7. 根据查询出的结果可以在界面上以表格和趋势图展示